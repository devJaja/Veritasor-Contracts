
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always

jobs:
  check:
    name: Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          components: rustfmt, clippy
          targets: wasm32-unknown-unknown

        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable
          components: rustfmt, clippy
          targets: wasm32-unknown-unknown

        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          components: rustfmt, clippy
          target: wasm32-unknown-unknown

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}-${{ env.RUST_STABLE || 'stable' }}
          restore-keys: |
            ${{ runner.os }}-cargo-

          fail-on-cache-miss: false
      
      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Run clippy
        run: cargo clippy --all-targets --all-features

      
      - name: Build attestation WASM (required for snapshot/revenue-stream contractimport)
        run: cargo build --release -p veritasor-attestation --target wasm32-unknown-unknown
      - name: Build snapshot WASM (required for aggregated-attestations contractimport)
        run: cargo build --release -p veritasor-attestation-snapshot --target wasm32-unknown-unknown
      
      - name: Run clippy
        run: cargo clippy --all-targets --all-features -- -D warnings

      - name: Check compilation
        run: cargo check --all-targets

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          targets: wasm32-unknown-unknown

        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable
          target: wasm32-unknown-unknown

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}-${{ env.RUST_STABLE || 'stable' }}
          restore-keys: |
            ${{ runner.os }}-cargo-

          fail-on-cache-miss: false
      
      - name: Build attestation WASM
        run: cargo build --release -p veritasor-attestation --target wasm32-unknown-unknown
      - name: Build snapshot WASM
        run: cargo build --release -p veritasor-attestation-snapshot --target wasm32-unknown-unknown
      
      - name: Run tests
        run: cargo test --all --verbose

  build:
    name: Build WASM
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          targets: wasm32-unknown-unknown

        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable
          target: wasm32-unknown-unknown

      - name: Ensure wasm32 target
        run: rustup target add wasm32-unknown-unknown

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}-${{ env.RUST_STABLE || 'stable' }}
          restore-keys: |
            ${{ runner.os }}-cargo-

          fail-on-cache-miss: false
      
      - name: Build attestation WASM
        run: cargo build --release -p veritasor-attestation --target wasm32-unknown-unknown
      - name: Build snapshot WASM
        run: cargo build --release -p veritasor-attestation-snapshot --target wasm32-unknown-unknown
      - name: Build contracts (release)
        run: cargo build --release --target wasm32-unknown-unknown

  wasm-size-budget:
    name: WASM Size Budget Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          targets: wasm32-unknown-unknown

      - name: Ensure wasm32 target
        run: rustup target add wasm32-unknown-unknown

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}-${{ env.RUST_STABLE || 'stable' }}
          restore-keys: |
            ${{ runner.os }}-cargo-

          fail-on-cache-miss: false

      - name: Build contracts (for size checking)
        run: |
          cargo build --release -p veritasor-attestor-staking --target wasm32-unknown-unknown
          cargo build --release -p veritasor-attestation-registry --target wasm32-unknown-unknown
          cargo build --release -p veritasor-audit-log --target wasm32-unknown-unknown
          cargo build --release -p veritasor-business-config --target wasm32-unknown-unknown
          cargo build --release -p veritasor-integration-registry --target wasm32-unknown-unknown
          cargo build --release -p veritasor-protocol-dao --target wasm32-unknown-unknown
          cargo build --release -p veritasor-protocol-simulation --target wasm32-unknown-unknown
          # Note: attestation, snapshot, aggregated-attestations, and revenue contracts
          # have compilation errors and are not built in this job

      - name: Run WASM size budget check
        run: |
          chmod +x scripts/check-wasm-sizes.sh
          ./scripts/check-wasm-sizes.sh --verbose